"""Indexing stub for semantic search project.

Usage: python index.py
"""

def build_index(data_path: str = "data/"):
    # Placeholder: load docs, compute embeddings, build FAISS index
    print(f"(stub) Building index from {data_path}")


if __name__ == "__main__":
    build_index()
